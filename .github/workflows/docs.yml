name: docs

on:
  pull_request:
  push:
    branches: [main]

jobs:
  markdown-smoke:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4
      - name: Verify expected docs exist
        run: |
          test -s README.md
          test -s docs/PRODUCT_BRIEF.md
          test -s docs/ARCHITECTURE.md
          test -s docs/ROADMAP.md
          test -s SECURITY.md
          test -s CONTRIBUTING.md
